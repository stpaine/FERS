# This file is the central manifest for the libfers public implementation. It defines:
#  - LIBFERS_PUBLIC_HEADERS: Headers for consumers of the library.

# --- Public Headers ---
set(LIBFERS_PUBLIC_HEADERS
	${CMAKE_CURRENT_LIST_DIR}/api.h
)
