Choosing to not use a git submodule for simplicity.

If an update is available for https://github.com/jothepro/doxygen-awesome-css, then the files in the main branch which
start with `doxygen-awesome` should be downloaded or copied into the `doxygen-awesome-css` directory to replace the
existing files.
